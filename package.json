{
  "name": "restaurant-qrmenu-server",
  "version": "1.0.0",
  "scripts": {
    "build": "npm install --prefix backend",
    "start": "node backend/server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
